#include<bits/stdc++.h>
using namespace std;

void solve(vector<int> &ques,vector<int> &ans){

}

int main(){
    int t;
    cin>>t;
    vector<int> ques;
    vector<int> ans;
    while(t--){
        int n;
        cin>>n;
        while(n--){
            int num;
            cin>>num;
            ques.push_back(num);
        }
        solve(ques,ans);
    }
}
